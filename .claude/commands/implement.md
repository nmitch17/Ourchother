# Implement the following plan
Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions
- Read the plan, think hard about the plan and implement the plan.
- When the plan is completed, run `/restart` to restart the dev server.

## Plan
$ARGUMENTS

## Report
- Summarize the work you've just done in a concise list that displays in the terminal as a "table" with the format:
   ----------------------------------
  | Task   | Status    | Description |
  |--------|-----------|-------------|
  | Task 1 | Completed | Description |
  | Task 2 | Completed | Description |
  | Task 3 | Completed | Description |
   ----------------------------------
- Report the files and total lines changed with `git diff --stat`
